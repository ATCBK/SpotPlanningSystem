# 《豫见河南 SpotPlanningSystem》答辩PPT详细讲解大纲

> 建议总时长：10-12 分钟（含 2 分钟实物演示视频）  
> 建议页数：12 页（结构完整、节奏紧凑）

## 0. 封面（第1页）
- 页面标题：豫见河南——基于最短路径思想的旅游智能规划系统
- 副标题：SpotPlanningSystem 课程项目答辩
- 展示信息：团队/姓名、课程名、日期
- 讲解话术（20s）：
  - 本项目聚焦“旅游路线怎么选、怎么走更合理”这个真实问题。
  - 我们实现了从景点选择、智能推荐、路径计算到行程输出的完整前端闭环。

## 1. 项目背景（第2-3页）

### 第2页：问题背景与需求来源
- 核心问题：
  - 用户做省内多景点出游时，常遇到“选点容易、排路线难”。
  - 人工规划依赖经验，常出现绕路、重复路线、时间预算不合理。
- 需求拆解：
  - 输入：起点、终点、必经景点。
  - 输出：可解释的路线顺序 + 总距离/时间/费用。
- 讲解话术（40s）：
  - 我们不是只做“景点展示网页”，而是把旅游规划抽象成可计算的问题。
  - 目标是让用户在几分钟内形成一条“说得清楚、算得明白”的行程方案。

### 第3页：项目目标与边界
- 目标：
  - 构建“选点 -> 推荐 -> 路径 -> 行程结果”5页业务链路。
  - 用图算法保证推荐逻辑可解释。
  - 支持课堂演示：无需后端、开箱可运行。
- 当前边界：
  - 使用本地 Mock 数据（河南 6 城市路网 + 12 个景点）。
  - 优化目标以“总距离最短”为主。
- 讲解话术（35s）：
  - 我们先把闭环跑通，再逐步扩展复杂度，这是课程项目最稳妥的落地路径。

## 2. 项目简介（第4-5页）

### 第4页：系统架构与技术栈
- 技术栈：Vue3 + TypeScript + Vite + Vue Router。
- 数据层：
  - `spots.ts`：景点静态数据。
  - `graph.ts`：城市图边（距离/时长/费用）。
- 状态管理：`planner.ts` 统一维护选点与当前路线方案，并通过 localStorage 持久化。
- 讲解话术（40s）：
  - 技术选型强调“轻量、可维护、便于演示”。
  - 虽是前端项目，但状态和算法层是解题核心，不只是页面堆砌。

### 第5页：业务流程与页面链路
- 五个页面：
  - `/home` 首页总览
  - `/spots` 景点总览（筛选与加入行程）
  - `/recommend` 智能推荐（起终点+必经点）
  - `/route` 路径详情（分段路径与总计指标）
  - `/result` 行程结果（汇总与分享文案）
- 业务流程图：
  - 用户输入约束 -> 生成推荐链路 -> 展示路径细节 -> 输出行程结果
- 讲解话术（45s）：
  - 这条链路保证用户每一步都有反馈，并且最终结果可追溯到前面的选择条件。

## 3. 核心算法介绍（第6-8页）

### 第6页：问题建模
- 建模方式：
  - 城市 = 图节点（Node）
  - 城市间通行关系 = 边（Edge）
  - 边权重包含：distance / time / cost
- 在当前版本中：
  - 主优化目标：distance（最短距离）
  - 同时累计输出 time、cost 用于结果解释
- 讲解话术（45s）：
  - 先把旅游问题转成图问题，算法才有统一输入与稳定输出。

### 第7页：最短路径计算（Dijkstra）
- 代码位置：`frontend/src/utils/dijkstra.ts`
- 关键流程：
  - 构建双向图 `buildGraph`
  - 初始化距离表、前驱节点表
  - 迭代选择当前最小未访问节点并松弛邻边
  - 回溯得到路径与分段 legs
- 结果输出：
  - `path`、`legs`、`totalDistance`、`totalTime`、`totalCost`
- 可放测试案例：
  - 郑州 -> 开封：78km，1.3h，¥95
  - 南阳 -> 安阳：南阳 -> 郑州 -> 安阳，总距离 312km
- 讲解话术（55s）：
  - 我们优先保证“路径正确 + 指标可核对”，让推荐结果可解释、可验证。

### 第8页：必经点重排策略（启发式插入）
- 代码位置：`frontend/src/state/planner.ts` 中 `reorderPassSpotsByShortestPath`
- 核心思想：
  - 对必经点逐个尝试插入到当前路径的不同位置
  - 计算“起点-必经点序列-终点”的总距离
  - 选择使总距离最小的插入位置
- 价值：
  - 用户只需给约束，不必手工排序必经点
  - 路径更短、演示效果更直观
- 讲解话术（55s）：
  - 这一步相当于在 Dijkstra 之上加一层业务优化，让“可用”变成“更好用”。

## 4. 实物演示视频（第9页）

### 第9页：演示视频脚本与镜头安排（建议 1分40秒 - 2分钟）
- 视频素材建议：使用你项目中的 5 页真实运行画面录屏（1080p，30fps）。
- 镜头脚本：
  - 00:00-00:15：`/home` 介绍项目目标与入口
  - 00:15-00:35：`/spots` 勾选景点（体现筛选/加入行程）
  - 00:35-01:05：`/recommend` 选择起点/终点/必经点，点击确认
  - 01:05-01:35：`/route` 展示城市路径动画与分段明细表
  - 01:35-01:55：`/result` 展示总里程/时长/费用与结果文案
- 配音要点：
  - 只强调“输入条件 -> 算法决策 -> 可解释结果”三件事。
- 讲解话术（20s）：
  - 接下来这段视频展示系统从用户输入到结果生成的完整闭环。

## 5. 优劣势分析（第10-11页）

### 第10页：项目优势
- 业务闭环完整：从选点到行程输出全流程可演示。
- 算法可解释：最短路径与分段指标可核算。
- 交互可视化：链路动画 + 地图节点激活，便于答辩展示。
- 工程可维护：TypeScript + 模块化数据/算法/页面结构清晰。
- 讲解话术（45s）：
  - 课程项目最关键是“讲得清 + 跑得通 + 能验证”，这三点我们都覆盖到了。

### 第11页：当前不足
- 数据规模偏小：当前仅 6 城市与 12 景点，泛化能力有限。
- 目标函数单一：以距离最短为主，尚未做个性化权重。
- 路网静态：未接入实时路况、天气、节假日拥堵。
- 架构层面：暂无后端服务，数据与策略都在前端 mock。
- 讲解话术（45s）：
  - 这些不足不是缺陷，而是下一阶段最明确的优化入口。

## 6. 未来改进方向（第12页）
- 算法升级：
  - 多目标优化（距离/时间/费用加权）
  - 必经点场景引入 TSP 近似策略（如 2-opt / 遗传算法）
  - 更大图规模时引入双向 Dijkstra 或 A* 提升效率
- 产品升级：
  - 接入用户偏好（亲子/文化/自然）做个性化推荐
  - 增加行程强度约束（每日时长、预算上限）
- 工程升级：
  - 增加后端 API 与数据库，支持真实景区与交通数据
  - 增加监控与回归测试，保障迭代稳定性
- 收尾话术（30s）：
  - 本项目已完成“可计算、可解释、可展示”的第一阶段目标，下一步将向“真实数据驱动的智能规划系统”演进。

---

## 附：答辩现场建议（可选备用页）
- 时间分配：背景1.5分钟 + 简介1.5分钟 + 算法3分钟 + 视频2分钟 + 分析与改进2分钟。
- 老师高频问题准备：
  - 为什么用 Dijkstra？
  - 必经点顺序如何决定？
  - 如果用户更在意费用而不是距离怎么办？
- 标准回答关键词：
  - 可解释、可扩展、可替换（策略层可升级）。
